set xterm_alt_key true
set show_hidden true

# preview_script runs in the same loader queue as copy/move; a slow scope.sh
# keeps has_work() true, so plain quit refuses forever
map q quit!
map Q quitall!
