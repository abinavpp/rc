[alias]
  cfl = config --local -l
  cfg = config --global -l
  lg0 = log --oneline
  sh0 = show --no-patch --oneline
  sh1 = show --stat --oneline
  st = status
  cm = commit
  cma = commit --amend
  cf = "!git add --all; git clang-format; git add --all; git reset"
  ch = checkout
  br = branch
  df = diff
  rb = rebase
  rbi = rebase -i
  rbc = rebase --continue
  dfc = diff --cached
  them = ls-files --others --directory

[init]
  defaultBranch = main

[pull]
  rebase = true

[include]
  path = ~/.post-gitconfig
