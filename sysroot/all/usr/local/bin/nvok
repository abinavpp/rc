#! /bin/bash

if ! systemctl status bumblebeed.service &> /dev/null; then
  if ! systemctl start bumblebeed.service &> /dev/null; then
    exit 1
  fi
fi

if type optirun &> /dev/null; then
  exit 0
else
  exit 1
fi
